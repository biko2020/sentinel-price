# =============================================================================
#  SentinelPrice · Python Dependencies
# =============================================================================
#  Install:  pip install -r requirements.txt
#  Python:   3.11+
# =============================================================================


# --- Scraping Engine ---------------------------------------------------------
scrapy==2.11.2


# --- JavaScript / Browser Rendering ------------------------------------------
# Required for JS-heavy product pages (lazy-loaded prices, SPAs)
scrapy-playwright==0.0.40
playwright==1.44.0


# --- Anti-Bot Middlewares -----------------------------------------------------
scrapy-rotating-proxies==0.6.2          # Rotating proxy pool support
scrapy-user-agents==0.1.1               # Random user-agent rotation
fake-useragent==1.5.1                   # User-agent database


# --- Database ----------------------------------------------------------------
psycopg2-binary==2.9.9                  # PostgreSQL adapter
sqlalchemy==2.0.30                      # ORM / query builder (optional layer)


# --- Data Cleaning & Validation ----------------------------------------------
itemloaders==1.3.1                      # Scrapy Item Loaders (ships with Scrapy)
price-parser==0.3.4                     # Robust price string parsing ($1,299.99 → 1299.99)
w3lib==2.1.2                            # URL and HTML utility functions


# --- Configuration & Environment ---------------------------------------------
python-dotenv==1.0.1                    # Load .env variables into os.environ


# --- Scheduling (optional) ---------------------------------------------------
# Uncomment if using Scrapyd for job scheduling
# scrapyd==2.1.1
# scrapyd-client==2.0.1


# --- Testing -----------------------------------------------------------------
pytest==8.2.1
pytest-asyncio==0.23.7